#include <stdio.h>

void printArray(int *a, int length) {
    for (int i = 0; i < length; i++) {
        printf("%d ", a[i]);
    }
}

int binary(int *arr, int n, int l, int h) {
    int mid = n / 2;

    
    
}

int main(int argc, char const *argv[]) {
    int a[] = {1, 2, 3, 4, 5};
    int n = sizeof a / sizeof a[0];
    int toFind = 5;


    


    return 0;
}
